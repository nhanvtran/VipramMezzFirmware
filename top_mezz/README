Steps to creating your own bitstream:

Copy this directory to your own:
$ cp -r top_mezz mymezz1
$ cd mymezz1

Then do the following to compile:
$ source setup_environment.sh
$ bash $REPOS_FW_DIR/ipbus/firmware/example_designs/scripts/setup.sh
$ cp ../trimac/tri_mode_eth_mac_v5_5* ipcore_dir/
$ xtclsh $REPOS_BUILD_DIR/build_project.tcl

